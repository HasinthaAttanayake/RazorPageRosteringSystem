@page
@model RazorPageRosteringSystem.Pages.Roles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Role[0].name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role[0].description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role[0].ratehr)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Role) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ratehr)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.roleID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.roleID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.roleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
