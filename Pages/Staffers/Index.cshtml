@page
@model RazorPageRosteringSystem.Pages.Staffers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].dateOfBirth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].mob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].Role)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Staff) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dateOfBirth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mob)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role.roleID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.staffID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.staffID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.staffID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
